name: pdcase_ideia_extra
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  path_provider: ^1.6.7
  sqflite: ^1.3.0
  flutter_launcher_icons: ^0.7.0
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  ios: true
  android: true
  image_path_ios: "imagens/pdcaselogo.png"
  image_path_android: "imagens/pdcaselogo.png"

flutter:
  uses-material-design: true
  assets:
    - imagens/pdcaselogo.png